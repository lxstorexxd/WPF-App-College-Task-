<Page x:Class="TestProject.Views.EmployeeShiftListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestProject.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EmployeeShiftListView" 
      Background="#F0F0F0">

    <Grid>
        <DataGrid ItemsSource="{Binding EmployeeShifts}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Employee" Binding="{Binding EmployeeName}" />
                <DataGridTextColumn Header="Shift Date" Binding="{Binding ShiftDate}" />
                <DataGridTextColumn Header="Shift Type" Binding="{Binding ShiftType}" />
                <DataGridTextColumn Header="Comments" Binding="{Binding Comments}" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Назад" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10" Height="24" Width="79" Click="Button_Click" />
    </Grid>
</Page>
